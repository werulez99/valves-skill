# Learned Patterns

Curated patterns promoted from post-audit `pattern_candidates.md` via the
post-audit improvement protocol. Agents may propose candidates during an audit;
only the curator/post-audit protocol may write files here.

See `~/.valves/methodology/pattern-candidate-schema.md` for the candidate format.
See `~/.plamen/rules/post-audit-improvement-protocol.md` for promotion rules.
